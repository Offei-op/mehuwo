# Slimmed dependency set for the Hugging Face Spaces deployment.
# The Space does NOT need:
#   - ollama (no daemon on the free tier)
#   - fastapi / uvicorn / sqlalchemy / python-multipart / python-dotenv
#     (no API or DB)
#   - torch / torchvision (CNN tick classifier; we use the heuristic)
#   - sympy (only used by the agent-side arithmetic verifier)
#   - dev tools (pytest, ruff, black, ipython, jupyter)
#   - optional charting (seaborn) or http (httpx, tqdm)

# UI + charts
streamlit
matplotlib

# Recognition pipeline (live demo on Page 3)
opencv-contrib-python
pillow
numpy
pypdfium2

# Data, scoring, clustering (for rendering bundled artefacts and
# re-running scoring/clustering on synthetic or uploaded data)
pandas
openpyxl
scipy
scikit-learn

# PDF generation (quiz, roster sheet, cluster + remediation reports)
reportlab

# Bank validation
jsonschema
